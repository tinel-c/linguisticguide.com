# LinguisticGuide Security Configuration
# Compatibility: Apache 2.4+ (eDomenii.ro)

# Prevent directory listing
Options -Indexes

# Protect sensitive files
<FilesMatch "^\.|\.(json|md|csv|lock|txt|php)$">
    # Exception for contact-handler.php which must be accessible
    <If "%{REQUEST_URI} != '/contact-handler.php'">
        Require all denied
    </If>
</FilesMatch>

# Security Headers (Secondary layer)
<IfModule mod_headers.c>
    Header set X-Frame-Options "SAMEORIGIN"
    Header set X-Content-Type-Options "nosniff"
    Header set X-XSS-Protection "1; mode=block"
    Header set Referrer-Policy "strict-origin-when-cross-origin"
</IfModule>

# Ensure UTF-8
AddDefaultCharset UTF-8
